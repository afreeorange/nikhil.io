{
  "scripts": {
    "build": "parcel build src/index.html --no-source-maps",
    "clean": "rm -rf dist/",
    "start": "parcel serve src/index.html"
  },
  "devDependencies": {
    "cssnano": "^4.1.10",
    "sass": "^1.32.8"
  },
  "dependencies": {}
}
