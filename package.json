{
  "name": "nikhil.io",
  "version": "3.1.0",
  "scripts": {
    "dev": "parcel 'src/*.pug'",
    "build": "pnpm run clean && parcel build 'src/*.pug' && cp src/assets/robots.txt dist/",
    "clean": "rm -rf dist"
  },
  "author": "Nikhil Anand",
  "license": "UNLICENSED",
  "private": true,
  "devDependencies": {
    "@parcel/transformer-pug": "2.12.0",
    "@parcel/transformer-sass": "2.12.0",
    "parcel": "^2.12.0"
  }
}
