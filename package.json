{
  "scripts": {
    "build": "yarn clean && npx parcel build src/index.html --no-source-maps && cp src/robots.txt dist/",
    "clean": "rm -rf dist/",
    "format": "npx prettier src/ --write",
    "start": "npx parcel src/*.html"
  },
  "source": "src/index.html",
  "devDependencies": {
    "parcel": "^2.8.1",
    "prettier": "^2.8.1"
  },
  "dependencies": {}
}
